name: Build Android APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Cordova
        run: npm install -g cordova

      - name: Build Android APK
        run: |
          npm install
          # পুরনো অ্যান্ড্রয়েড প্ল্যাটফর্ম মুছে নতুন করে ফ্রেশভাবে অ্যাড করা
          cordova platform remove android || true
          cordova platform add android@12.0.1
          
          # লাইসেন্স অটো-একসেপ্ট করা
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses || true
          
          # মেমোরি ফিক্স
          mkdir -p ~/.gradle
          echo "org.gradle.jvmargs=-Xmx2048m" > ~/.gradle/gradle.properties
          
          # বিল্ড কমান্ড (সরাসরি করডোভা ব্যবহার করে)
          cordova build android --debug --device -- --packageType=apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: my-p2p-app
          path: platforms/android/app/build/outputs/apk/debug/app-debug.apk
          
